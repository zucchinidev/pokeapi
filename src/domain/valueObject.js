/**
 * @interface
 * Permite modelar un concepto que es importante para el dominio de la app pero que no tiene
 * identidad ni es único, por ejemplo: Precio, Fecha...
 */
export default class ValueObject {}